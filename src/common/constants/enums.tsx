// Alert Style

export enum AlertStyle {
  cancel = 'cancel',
  default = 'default',
  destructive = 'destructive',
}

export enum UseFormMode {
  ON_CHANGE = 'onChange',
  ON_SUBMIT = 'onSubmit',
  ON_TOUCHED = 'onTouched',
  ALL = 'all',
  ON_BLUR = 'onBlur',
}

export enum InputBorderType {
  RECTANGULAR = 1,
  BOTTOM_ONLY = 2,
  RADIUS = 4,
  NONE = 5,
}
